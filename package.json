{
  "name": "flamingo-football-rescue",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "An independent football rescue action game with original Flamingo characters.",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 4208 --strictPort",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4208 --strictPort",
    "test": "node --test tests/*.test.mjs",
    "capture": "node scripts/capture.mjs"
  },
  "engines": { "node": ">=22.12.0" },
  "devDependencies": { "vite": "8.2.2", "@napi-rs/canvas": "0.1.80", "galmuri": "2.40.3" }
}
